# ADR NNNN: <title>

Date: YYYY-MM-DD
Status: proposed | ratified (owner, YYYY-MM-DD) | superseded-by-NNNN

## Context

The problem this decision responds to, in 1-2 paragraphs. Cite the
evidence that made it a decision rather than an obvious default (an issue
number, a failure that actually happened, a design comment) — an ADR
records *why*, not just *what*.

## Decision

What was decided, stated plainly. If alternatives were seriously
considered, name them and why they lost in a sentence or two each — this
section doesn't need the platform's full "Alternatives considered" essay,
just enough that a future reader isn't left re-litigating a settled
question.

## Consequences

- What this binds — what future work must now honor because of this
  decision.
- What this forecloses — options that are now off the table unless this
  ADR is superseded.
- Any delete-when condition — a fact that, if it changes, means this ADR
  should be revisited or superseded (optional; not every decision has one).
