# Entity Candidate goldens

`fixtures.json` contains synthetic prompt inputs, model proposals, and expected
outcomes. `sample_predictions.json` is the deterministic recorded seat. Live
seating uses the same fixtures and gates and skips loudly without a configured
provider.
